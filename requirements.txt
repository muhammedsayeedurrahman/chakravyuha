# Slim requirements for Render free tier
# Only essentials for API to start and serve requests

# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
pydantic>=2.0.0
email-validator>=2.0.0

# HTTP clients (LLM API calls)
httpx>=0.25.0

# Voice (Sarvam AI SDK — lightweight HTTP wrapper)
sarvamai>=0.1.27

# Web requests
requests>=2.31.0
beautifulsoup4>=4.12.0

# Browser Automation (OpenClaw) — optional, lazy-imported, graceful degradation
# playwright>=1.40.0  # Heavy (~50MB), enable only if OpenClaw browser filing needed
google-generativeai>=0.8.0
