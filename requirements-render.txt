# Slim requirements for Render free tier
# Only essentials for API to start and serve requests

# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# HTTP clients (LLM API calls)
httpx>=0.25.0

# Keyword search (lightweight RAG fallback)
rank-bm25>=0.2.2

# Web requests
requests>=2.31.0
beautifulsoup4>=4.12.0
