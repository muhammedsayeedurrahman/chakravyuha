# Slim requirements for Render free tier deployment
# Excludes heavy ML deps (torch, chromadb, sentence-transformers)
# These features degrade gracefully when missing

# Core Framework
fastapi>=0.104.0
uvicorn>=0.24.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP clients (for LLM API calls)
httpx>=0.25.0
aiohttp>=3.9.0
requests>=2.31.0

# Sarvam AI (voice features - lightweight SDK)
sarvamai>=0.1.27

# Web scraping (corpus)
beautifulsoup4>=4.12.0

# Indian NLP
indic-nlp-library>=0.81

# BM25 keyword search (lightweight RAG fallback)
rank-bm25>=0.2.2

# Audio conversion
pydub>=0.25.1
imageio-ffmpeg>=0.4.9
